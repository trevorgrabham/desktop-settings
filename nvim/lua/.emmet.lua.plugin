return {
  "mattn/emmet-vim",
  event = "InsertEnter",
  config = function ()
    vim.g.user_emmet_leader_key = "<C-y>"
    vim.g.user_emmet_settings = {
      templ = { extends = "html" },
    }
  end,
}
